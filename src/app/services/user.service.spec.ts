/*
import { UserService } from './user.service';

describe('UserService', () => {
    it('should greet known users', () => {
        const svc = new UserService();
        expect(svc.greet('admin')).toContain('admin');
        expect(svc.greet('demo')).toContain('demo');
    });
});
*/
